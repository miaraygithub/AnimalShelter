public interface AdoptableAnimal { //this is an interface, not a class (first time not turning in non classes, wooo)
    String getDescription(); //we need a getDescription() 'template' that returns a string for the classes to use
    String getName(); //we need a getDescription() 'template' that returns a string for the classes to use
    double getPrice(); //we need a getDescription() 'template' that returns a string for the classes to use
}
